export enum ProductReviewModerationStatusEnum {
    // the product review is currently being reviewed by the moderation
    PENDING = 'pending',
    // the product review has been approved
    APPROVED = 'approved',
    // the product review has been rejected
    REJECTED = 'rejected',
}
