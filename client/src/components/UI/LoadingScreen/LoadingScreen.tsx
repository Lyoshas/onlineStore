import Loading from '../Loading/Loading';

const LoadingScreen = () => {
    return (
        <div className="flex-wrapper">
            <Loading />
        </div>
    );
};

export default LoadingScreen;
